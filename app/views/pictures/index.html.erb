<h2>Your Picture Gallery</h2>
<div class="row well pic_show pic_back">
  <div class="col-md-4" id="journal_directions">
    <h3>This is where you can curate your pictures for a given trove. You can add pictures as well as remove pictures that you already have added.</h3>
    <%= link_to "Add Pictures", new_box_picture_path, class: 'btn btn-primary btn-xs' %>
    <%= link_to "Trove", box_path(@box), class: 'btn btn-primary btn-xs' %>
  </div>

  <div class="col-md-7 col-md-offset-1" id="pic_table">
    <% @pictures.each do |pic| %>
          <table class="table table-hover well">
            <thead>
              <tr>
                <th class="col-md-6">Caption</th>
                <th class="col-md-6">Thumbnail</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><%= pic.caption %></td>
                <td><%= link_to image_tag(pic.photo.thumb.url), [@box, pic], class: 'img-responsive center-block' %></td>
              </tr>
            </tbody>
          </table>
    <% end %>
  </div>
</div>
