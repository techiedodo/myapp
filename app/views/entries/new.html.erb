<h1>Your New Entry</h1>

 <div class="row well journal_index">
   <div class="col-md-4" id="journal_directions">
     <h4>Guidelines for entry</h4>
     <ul>
       <li>Make sure it rhymes.</li>
       <li>Don't use the letter "A".</li>
       <li>The incessant use of hashtags will get you banned.</li>
     </ul>
   </div>
   <div class="col-md-8 well">
     <%= form_for [@journal, @entry] do |f| %>
       <div class="form-group">
         <%= f.label :subject %>
         <%= f.text_field :subject, class: 'form-control', placeholder: "What's the subject?" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, class: 'form-control', placeholder: "Type away..." %>
       </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
         <%= link_to "Cancel", journal_entries_path , class: 'btn btn-danger' %>
       </div>
     <% end %>
   </div>
 </div>
