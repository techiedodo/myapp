<h1>Your Troves</h1>
<div class="row well trove_main">
  <div class="col-md-4">
    <h4 id="trove_directions">This is where you begin the creation of your trove. After the trove is created, you can begin to add itmes to it as well as assign recipients to your trove.</h4>
    <%= link_to "Create Trove", new_box_path, class: 'btn btn-primary btn-xs' %>
  </div>

  <div class="col-md-7 col-md-offset-1">
    <% @boxes.each do |box| %>
      <div class="row media well well-sm">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to box.title, box, class: 'col-xs-9 col-md-9' %>
            <%= link_to "Delete Trove", box, method: :delete, class: 'btn btn-danger btn-xs col-xs-3 col-md-2 col-md-offset-1', data: { confirm: 'Are you sure you want to delete this Trove?' } %>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
</div>
