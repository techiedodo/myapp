<h1>Your Journals</h1>
<div class="row well journal_index">
  <div class="col-md-3">
    <h3 id="trove_directions">This is where you can curate your journals for a given trove. You can create journals as well as continue to develop journals that you already have.</h3>
    <%= link_to "Create Journal", new_box_journal_path, class: 'btn btn-primary btn-xs' %>
    <%= link_to "Trove", box_path(@box), class: 'btn btn-primary btn-xs' %>
  </div>

  <div class="col-md-7 col-md-offset-1">
    <% @jrnl4box.each do |journal| %>
      <div class="row media well well-sm">
        <div class="media-body">
          <h4 class="media-heading">
            <%= journal.title %>
              <h6 class="col-xs-9 col-md-9"><%= link_to "New Entry", new_journal_entry_path(journal, @entry)%> |
              <%= link_to "View Entries", journal_entries_path(journal)%> |
              <%= link_to "Edit Journal", edit_box_journal_path(@box, journal)%></h6>
              <h6 class="col-xs-3 col-md-2 col-md-offset-1"><%= link_to "Delete", [@box, journal], method: :delete, class: 'btn btn-danger btn-xs', data: { confirm: 'Are you sure you want to delete this journal?' } %></h6>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
</div>
